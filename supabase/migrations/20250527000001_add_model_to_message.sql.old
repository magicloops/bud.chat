-- Add model column to message table
-- alter table message add column model text;

-- Create index for model queries
-- create index idx_message_model on message(model);

-- NOTE: Stored in metadata?
